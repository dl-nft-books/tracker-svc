allOf:
  - $ref: '#/components/schemas/StatisticsKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - book_id
          - chain_id
          - amount
        properties:
          book_id:
            type: integer
            format: int64
          chain_id:
            type: integer
            format: int64
          amount:
            type: integer
            format: int64